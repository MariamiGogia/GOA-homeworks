#task 3
#tuple is like list, it is ordered collections of items and its immutable
# We use it when we want to write a list, that needs to be same and can't be changed
#It has indexing (start-0) and u can do everything except things that contains modifying them.